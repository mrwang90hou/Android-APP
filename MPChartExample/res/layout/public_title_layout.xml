<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/title_layout"
    android:layout_width="match_parent" android:layout_height="36dp"
    android:background="@color/text_top_status_color">

    <!-- left -->

    <ImageView
        android:id="@+id/title_left_btn_img"
        style="@style/all_back_style" />

    <TextView
        android:id="@+id/title_left_tv"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:gravity="center"  android:text="取消" android:textColor="@android:color/white"
        android:paddingLeft="15dp"
        android:visibility="gone" />
    <!-- right -->

    <ImageView
        android:id="@+id/title_right_btn_img"
         android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:padding="14sp"
        android:layout_centerVertical="true"
        android:src="@drawable/back" />

    <TextView
        android:id="@+id/title_right_tv"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:gravity="center" android:text="保存" android:textColor="@android:color/white"
        android:paddingRight="15dp" android:singleLine="true"
        android:visibility="gone" />
    <!-- middle -->

    <TextView
        android:id="@+id/title_middle_tv"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerInParent="true" android:drawablePadding="8dp"
        style="@style/title_middle_text"
        android:textStyle="normal"
        android:text="@string/app_name" />

</RelativeLayout>